// notacion
// ;  -->Delimitar el final de una linea
const b = 4;
b + 4;

// . --> se utiliza en los objetos para acceder a los atributos ( ejemplo )
// movil.anchura

/* [] --> se utilizan para (listas, arreglos o arrays como mejor nos parezca)
se utilizan para definir los arrays o para acceder a un valor dentro de la arrays: 2 ejemplos abajo*/
const ar = [1,2,3,4,5,]; //  definir
console.log (ar [2]); // acceder: recordemos que se empieza acontar desde 0

// () -->  se utiliza para las funciones
function suma(a, b){// se escribe la funcion
}

// {} se utilizan para objetos, funciones y estruturas de control
const movil = {
    altura : 10, 
    anchura : 5, // objetos
}
if (true){
    // todo lo que haya entre llaves


const constante2 = "hola"

}